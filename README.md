# Playwright Apextech project

## Install Playwright

Run command:
```
npm ci
```
## Run autotests

To run only LOGIN autotests - run command:

```
npm run login
```

To run only API autotests - run command:

```
npm run api
```

